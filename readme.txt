la configuration 
    .env 

node-module
    packagejson (module + lecture du serveur back)

installation des dépendances back et front 
    npm install sur le back
    npm install sur le front 

démarage back-end 
    nodemon server

démarage front-end
    npm start